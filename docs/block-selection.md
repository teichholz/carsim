Blocks can be selected.
- left clicking a blocks hightlight the square quadrant it is contained
- ctrl + left click creates a selection area which allows to highlight an rectangle area
- shift + click allows to select multiple blocks.
- When Blocks are selected they can be moved as a whole and can be deleted with backspace.
ctrl-z / ctrl-r should be able to undo / redo these operations
